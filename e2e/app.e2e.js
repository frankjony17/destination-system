/**
 * Created by gabriel on 29/08/16.
 */
/*describe('Tela inicial', function () {

  beforeEach(function () {
    browser.get('');
  });

  it('Deveria acessar o menu', function() {
    //browser.actions().mouseMove(element(by.css('.menu'))).perform();
    // element(by.css('.menu')).click();
    // element(by.name('teste')).click();
    //browser.sleep( 5000 );
    //element(by.xpath('//a[@ui-sref="inserirServicos"]')).click();
    // browser.getCurrentUrl().then(function (url) {
    //   expect(url).toMatch('/servicos/cadastrar');
    // });
  });
});*/
